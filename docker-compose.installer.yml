services:
  installer:
    container_name: azuracast_installer
    image: 'giva9712/boostcast:v3'
    volumes:
      - './:/installer'
    restart: 'no'
    entrypoint: docker_installer
    command: install
    privileged: true
